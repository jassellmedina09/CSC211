//  Implement an algorithm in C++ to test if a number is within a range given by the user, using a single if statement. This should take three values from the user those being (in order) the lower bound on the range, the upper bound on the range, and then the number to be tested.
#include <iostream>

int main() {
int low, high, test;

std::cout << "Input 3 numbers:";
std::cin >> low >> high >>

}